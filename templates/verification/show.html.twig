{% extends 'base.html.twig' %}

{% block title %}Verification{% endblock %}

{% block body %}
    <h1>Verification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ verification.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ verification.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_verification_index') }}">back to list</a>

    <a href="{{ path('app_verification_edit', {'id': verification.id}) }}">edit</a>

    {{ include('verification/_delete_form.html.twig') }}
{% endblock %}
