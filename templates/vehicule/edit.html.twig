{% extends 'base.html.twig' %}

{% block title %}Modifier un véhicule{% endblock %}

{% block body %}
    <div class="container d-flex flex-column justify-content-center col-6">
        <br>
        <h1 class="text-center">Modification du véhicule</h1>
        <br>
        <div class="alert-success rounded text-center">
            {{ confirmation }}
        </div>
        <div class="alert-danger rounded text-center">
            {{ error }}
        </div>
        <br>
        {{ include('vehicule/_form.html.twig', {'button_label': 'Valider'}) }}
        <br>
    </div>
{% endblock %}
